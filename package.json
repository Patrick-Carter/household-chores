{
  "name": "household-chores",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c green,yellow \"npm -w @chores/server run dev\" \"npm -w @chores/client run dev\"",
    "build": "npm -w @chores/shared run build && npm -w @chores/server run build && npm -w @chores/client run build",
    "start": "node server/dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc -b shared server client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
